<!-- dns-soc-nav:start -->
[🏠 Repository Home](../README.md) · [📁 03 Splunk Build](README.md)
<!-- dns-soc-nav:end -->

<img src="https://user-images.githubusercontent.com/73097560/115834477-dbab4500-a447-11eb-908a-139a6edaec5c.gif" width="100%" alt="section divider" />

# Scenario 02 Resolver & Sinkhole Onboarding

**Status:** Complete — infrastructure data-quality gate passed  
**Implementation owner:** [_Abdul-Rehman_](https://github.com/abdul4rehman215)  
**Resolver:** `dns-soc-resolver01` — `10.50.30.10`  
**Sinkhole:** `dns-soc-sinkhole01` — `10.50.30.30`  
**Splunk receiver:** `10.50.20.10:9997`

This document records the real Scenario 02 infrastructure telemetry that later supported the completed DGA/high-NXDOMAIN Detection Engineering, SOC, and IR workflow. Scenario-specific detection logic remains in the dedicated Scenario 02 repository.

## Resolver collection path

```text
Unbound query/reply/RPZ logs
        |
        v
system syslog
        |
        v
/var/log/dns-soc/unbound.log
        |
        | Universal Forwarder 10.4.2 / TCP 9997
        v
dns-soc-splunk01
        |
        v
index=dns_soc_dns
sourcetype=unbound:dns
host=dns-soc-resolver01
```

The dedicated file is intentionally narrower than forwarding all of `/var/log/syslog`.

## Resolver Universal Forwarder

The resolver UF was installed and pointed at the existing private receiver.

![Resolver active forward](screenshots/scenario-02/88-resolver-uf-active-forward.png)

*`10.50.20.10:9997` is the active forward destination.*

Real Unbound events were first proven in the dedicated file:

![Dedicated Unbound log validation](screenshots/scenario-02/89-resolver-dedicated-log-validation.png)

*The file contains real query/reply events generated by `10.50.30.20`.*

The repository-safe input is kept in [`forwarders/dns-soc-resolver01/inputs.conf`](forwarders/dns-soc-resolver01/inputs.conf):

```ini
[monitor:///var/log/dns-soc/unbound.log]
disabled = false
index = dns_soc_dns
sourcetype = unbound:dns
host = dns-soc-resolver01
```

![Resolver UF input](screenshots/scenario-02/90-resolver-uf-input-config.png)

## Resolver data-quality validation

The final identity is:

| Property | Validated value |
|---|---|
| Index | `dns_soc_dns` |
| Host | `dns-soc-resolver01` |
| Source | `/var/log/dns-soc/unbound.log` |
| Sourcetype | `unbound:dns` |
| Time | Matches real DNS activity |

![Resolver events in Splunk](screenshots/scenario-02/91-resolver-events-in-splunk.png)

*Real query/reply events, client identity and `NXDOMAIN` results are searchable in the intended index.*

## Persistent core DNS fields

The field extraction was created in Splunk Web for `sourcetype=unbound:dns` after validating the regex against real events.

Extraction name:

```text
unbound_dns_core_fields
```

Regex:

```regex
unbound:\s+\[[^\]]+\]\s+(?<event_type>query|reply):\s+(?<client_ip>\S+)\s+(?<qname>\S+)\s+(?<qtype>\S+)\s+IN(?:\s+(?<rcode>[A-Z]+))?
```

Validated fields:

```text
event_type
client_ip
qname
qtype
rcode
```

![Resolver field validation](screenshots/scenario-02/92-resolver-field-validation.png)

*The core fields are available without repeating `rex` in every search.*

## Persistent reply metrics

Extraction name:

```text
unbound_dns_reply_metrics
```

Regex:

```regex
IN\s+(?<rcode>[A-Z]+)\s+(?<response_time>[0-9.]+)\s+(?<cache_flag>\d+)\s+(?<response_size>\d+)$
```

Validated additional values:

```text
response_time
cache_flag
response_size
```

`cache_flag` is preserved as the raw numeric value. This repository does not assign a semantic label that was not separately validated.

![Resolver reply metrics](screenshots/scenario-02/93-resolver-reply-metrics.png)

*Reply timing and response-size fields extract consistently for real `NOERROR` and `NXDOMAIN` events.*

`transport` is not directly present in this Unbound text log format and is therefore not invented as a field. RPZ behavior is available in the raw resolver event text; a normalized `rpz_action` field has not been claimed yet.

## Sinkhole collection path

```text
dns-soc-sinkhole01
Nginx /var/log/nginx/access.log
        |
        | Universal Forwarder 10.4.2 / TCP 9997
        v
dns-soc-splunk01
        |
        v
index=dns_soc_web
sourcetype=nginx:access
host=dns-soc-sinkhole01
```

The actual sinkhole sourcetype is `nginx:access`. It is intentionally documented as deployed rather than renamed to match the public Web host convention.

The repository-safe input is [`forwarders/dns-soc-sinkhole01/inputs.conf`](forwarders/dns-soc-sinkhole01/inputs.conf).

![Sinkhole UF input](screenshots/scenario-02/95-sinkhole-uf-input-config.png)

A controlled nonexistent HTTP path was requested only to prove ingestion. The `404` appeared in Splunk with the correct source client, host, source and sourcetype:

![Sinkhole events in Splunk](screenshots/scenario-02/96-sinkhole-events-in-splunk.png)

## RPZ telemetry validation

The infrastructure test first kept RPZ enforcement disabled. The resolver still logged the policy match, and the event reached `dns_soc_dns`:

![RPZ safe match in Splunk](screenshots/scenario-02/98-rpz-safe-match-in-splunk.png)

*The RPZ match is searchable before any DNS response is changed.*

During the controlled redirect window, the victim reached Nginx through the RPZ-selected sinkhole. The corresponding HTTP event appeared in Splunk:

![Sinkhole HTTP evidence in Splunk](screenshots/scenario-02/101-rpz-sinkhole-http-in-splunk.png)

*The sinkhole access event provides independent application evidence for the containment path.*

## Reusable validation SPL

The searches used for this onboarding are appended to [`validation/validation-searches.spl`](validation/validation-searches.spl).

Useful reusable resolver pivot:

```spl
index=dns_soc_dns host="dns-soc-resolver01" sourcetype="unbound:dns"
| where isnotnull(event_type)
| table _time event_type client_ip qname qtype rcode response_time cache_flag response_size
| sort - _time
```

This is an onboarding/data-quality search, not the final DGA detection.

## Data-quality gate

```text
Resolver UF private path             PASS
Resolver index/host/source/type      PASS
Real NOERROR/NXDOMAIN events         PASS
Core DNS fields                      PASS
Reply metrics                        PASS
Sinkhole UF private path             PASS
Sinkhole access events               PASS
RPZ safe-match telemetry             PASS
RPZ redirect + HTTP evidence         PASS
```

`dns_soc_dns` passed the infrastructure data-quality gate and was later used by the Scenario 02 baseline, Detection v1.0, SOC investigation, IR validation, and RPZ before/after verification. The scenario-specific thresholds and final evidence remain in the Scenario 02 repository.

<img src="https://user-images.githubusercontent.com/73097560/115834477-dbab4500-a447-11eb-908a-139a6edaec5c.gif" width="100%" alt="section divider" />

<!-- dns-soc-footer:start -->
<div align="center">

[🏠 Repository Home](../README.md) · [📁 03 Splunk Build](README.md)

<sub>DNSentinel Lab · Controlled DNS security training documentation</sub>

</div>
<!-- dns-soc-footer:end -->
